import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import { StackNavigatorDTO } from '../../../models/StackNavigatorDTO';

export type HomeDTO = NativeStackScreenProps<StackNavigatorDTO, 'Home'>;